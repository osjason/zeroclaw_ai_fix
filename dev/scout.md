project: Rust 单仓（workspace: `.` + `crates/robot-kit`），核心在 `src/`，以 trait/factory 组织；当前分支 `release/v0.1.8`，从 `493d1aab` 到 `HEAD` 已有 68 个增量提交，近期集中在 `agent/channels/cron/security` 的进度事件与安全拦截统一。
entrypoints: [src/main.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/main.rs), [src/lib.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/lib.rs), [src/agent/loop_.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/agent/loop_.rs), [src/channels/mod.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/channels/mod.rs), [src/channels/lark.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/channels/lark.rs), [src/channels/progress_event.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/channels/progress_event.rs), [src/cron/scheduler.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/cron/scheduler.rs), [src/security/policy.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/security/policy.rs), [src/tools/mod.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/tools/mod.rs), [src/tools/cron_add.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/tools/cron_add.rs), [src/tools/cron_update.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/tools/cron_update.rs), [src/config/schema.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/config/schema.rs)。
verify: 最小验证建议 3 条定点测试；已实跑 1 条并通过：`cargo test channels::tests::effective_progress_mode_defaults_non_telegram_to_off -- --exact`。下一步最小集可用 `cargo test tools::cron_add::tests::blocks_disallowed_shell_command -- --exact` + `cargo test cron::scheduler::tests::process_due_jobs_blocked_command_delivers_trigger_running_then_blocked_with_policy -- --exact`。
risks: 1) `cargo test ... --exact` 仍会编译/运行大量测试二进制（本次输出可见 `stress_test_*` 二进制也被启动），测试资源压力仍高。2) Feishu/Lark 进度默认已偏向 `Compact`，但是否“持续可见”仍受 draft 更新节流与配置影响。3) `cron_add/cron_update` 仍有一段安全校验返回普通字符串，未完全走结构化 `policy/command/reason` 事件，可能影响拦截汇报一致性。4) 大文件集中（如 `schema.rs`/`channels/mod.rs`/`loop_.rs`）使重构回归风险高。
first_subgoal: 一次性做完 5 个任务再提交。先统一 `cron_add + cron_update + scheduler` 的命令安全预检到一个共享抽象（复用 `CommandPolicyViolation` 结构化输出），删除重复分支并统一拦截事件格式，再用上述 2-3 条定点测试回归；这是当前“降代码量 + 保兼容 + 改善拦截可解释性”的最小切口。