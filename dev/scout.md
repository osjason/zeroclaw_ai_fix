project: Rust 2021 单仓（workspace: `.` + `crates/robot-kit`），主工程在 `src/`，核心是 trait/factory 架构；渠道、工具、安全、cron、agent loop 都在同一可执行体里，默认启用 `channel-lark`。  
entrypoints: [src/main.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/main.rs), [src/lib.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/lib.rs), [src/channels/mod.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/channels/mod.rs), [src/channels/lark.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/channels/lark.rs), [src/agent/loop_.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/agent/loop_.rs), [src/cron/scheduler.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/cron/scheduler.rs), [src/security/policy.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/security/policy.rs), [src/config/schema.rs](C:/Users/osjas/MyData/PG/zeroclaw/src/config/schema.rs)。  
verify: 最小验证先跑 3 个定点测试：`cargo test channels::tests::effective_progress_mode_defaults_non_telegram_to_off`、`cargo test cron::scheduler::tests::run_job_command_blocks_disallowed_command`、`cargo test tools::cron_update::tests::applies_default_delivery_to_agent_jobs_when_patch_omits_it`。  
risks: 1) Feishu/Lark 虽支持 draft 更新，但非 Telegram 默认 `ProgressMode::Off`，导致“执行中进度”默认不可见。2) cron announce 主要在执行后投递输出，缺少“触发即通知/执行中通知”统一路径。3) 安全拦截文案在多个模块分散，汇报粒度不一致。4) `allowed_commands="*"` 也仍受 shell 结构拦截（重定向/子命令等），容易被误解成“全放行”。  
first_subgoal: 先做“可复用抽象”切口：抽离 cron/schedule 共用的 mutation guard + 安全拦截原因标准化（统一错误结构/文案），先降重复代码量，再把它接入 Feishu/cron 通知链。